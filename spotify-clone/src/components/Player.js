import React from 'react';
import './Player.css';

function Player({ spotify }) {
    return (
        <div className="player">
            {/* Sidebar */}
            {/* Body */}
            {/* Footer */}
        </div>
    )
}

export default Player
